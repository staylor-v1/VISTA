apiVersion: apps/v1
kind: Deployment
metadata:
  name: vista
spec:
  selector:
    matchLabels:
      app: vista
  template:
    metadata:
      labels:
        app: vista
    spec:
      containers:
      - name: app
        image: vista:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: app-config
        - secretRef:
            name: app-secrets
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: vista
spec:
  type: NodePort
  selector:
    app: vista
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30080